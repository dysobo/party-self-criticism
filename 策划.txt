党员自我批评自动生成--自助页

页面上元素：
1、设定
可以填写姓名、岗位、主要工作内容（提示越详细越好）、检验/计量的主要产品

2、有哪些自己想说的（这些将会是重点，不写就系统默认生成）

3、一键生成按钮

4、一键导出至word文档（注意排版）

5、局部修改要求

6、完全不满意，重新生成按钮

7、内容生成使用ai，内置一个免费模型gpt-4o-free
apikey（页面不显示）：sk-3qejSsOvN3wbmVKR6a81C2056bEc498e8373CfC8C922Bf04

调用示例
python：
import openai

client = openai.OpenAI(
  api_key="<AIHUBMIX_API_KEY>",  # 换成你在 AiHubMix 生成的密钥
  base_url="https://aihubmix.com/v1"
)

response = client.chat.completions.create(
  model="gpt-4o-free",
  messages=[
      {"role": "user", "content": "Hello, how are you?"}
  ]
)

print(response.choices[0].message.content)

Typescript：
fetch('https://aihubmix.com/v1/chat/completions', {
method: 'POST',
headers: {
  'Authorization': 'Bearer <AIHUBMIX_API_KEY>',
  'Content-Type': 'application/json',
},
body: JSON.stringify({
  model: "gpt-4o-free",
  messages: [
    {
      role: "user",
      content: "Hello, how are you?"
    }
  ]
})
})
.then(response => response.json())
.then(data => console.log(data.choices[0].message.content))

curl：
curl https://aihubmix.com/v1/chat/completions \\
-H "Content-Type: application/json" \\
-H "Authorization: Bearer <AIHUBMIX_API_KEY>" \\
-d '{
  "model": "gpt-4o-free",
  "messages": [
    {
      "role": "user",
      "content": "Hello, how are you?"
    }
  ]
}'

Response-api：
from openai import OpenAI

client = OpenAI(
    api_key="<AIHUBMIX_API_KEY>", # 换成你在后台生成的 Key "sk-***"
    base_url="https://aihubmix.com/v1"
)

response = client.responses.create(
    model="gpt-4o-free",
    input="塔罗占卜为何有效，背后的原理和可迁移的方法是什么？Output format: Markdown",
    reasoning={
        "effort": "minimal"
    },
    text={
        "verbosity": "low"
    },
    stream=True
)

for event in response:
    print(event)